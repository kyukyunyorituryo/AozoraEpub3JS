<?xml version="1.0" encoding="UTF-8"?>
<ncx xmlns="http://www.daisy.org/z3986/2005/ncx/" version="2005-1">
<head>
    <meta name="dtb:uid" content="urn:uuid:<%= identifier -%>"/>
    <meta name="dtb:depth" content="<%= ncx_depth -%>"/>
    <meta name="dtb:totalPageCount" content="0"/>
    <meta name="dtb:maxPageNumber" content="0"/>
</head>
<docTitle>
    <text><%= title -%></text>
</docTitle>
<navMap>
<% let idx = 0; -%>
<% if (bookInfo.InsertCoverPage && bookInfo.InsertCoverPageToc) { -%>
    <% idx++; -%>
    <navPoint id="toccover" playOrder="<%= idx %>">
        <navLabel>
            <text><%= cover_name -%></text>
        </navLabel>
        <content src="xhtml/cover.xhtml"/>
    </navPoint>
<% } -%>
<% for(var i = 0; i < chapters.length; i++) { -%>
    <% if (chapters[i].ChapterName) { -%>
        <% idx++; -%>
        <navPoint id="toc<%= idx -%>" playOrder="<%= idx -%>">
            <navLabel>
                <text><%= chapters[i].NoTagChapterName -%></text>
            </navLabel>
            <% if (chapters[i].ChapterId) { -%>
                <content src="xhtml/<%= chapters[i].SectionId -%>.xhtml#<%= chapters[i].ChapterId -%>"/>
            <% } else { -%>
                <content src="xhtml/<%= chapters[i].SectionId -%>.xhtml"/>
            <% } -%>
        </navPoint>
    <% } -%>
<% }; -%>
</navMap>
</ncx>
